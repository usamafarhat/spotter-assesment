/** Shared column width for mobile bottom sheets. */
export const APP_SHELL_WIDTH_CLASS = "w-full max-w-md";

/** Centered content well used by list/dashboard pages on desktop. */
export const PAGE_GUTTER_CLASS = "mx-auto w-full max-w-6xl px-5 md:px-6 lg:px-8";

/** Trip cards fill the row, then wrap as the viewport widens. */
export const TRIP_CARD_GRID_CLASS =
  "grid gap-2.5 [grid-template-columns:repeat(auto-fit,minmax(min(100%,18rem),1fr))]";
